/**
 * Shared motion constants for consistent animations across components.
 * Uses a custom ease-out curve for smooth, refined transitions.
 */

/** Smooth ease-out curve: fast start, gentle deceleration */
export const easeOut = [0.16, 1, 0.3, 1] as const;
