services:
  - type: web
    name: unisage-backend
    env: node
    branch: main
    root: unisage_main/backend
    # Explicitly run commands from the backend folder to avoid npm ci running at repo root
    buildCommand: cd unisage_main/backend && npm ci
    startCommand: cd unisage_main/backend && npm start
    plan: free